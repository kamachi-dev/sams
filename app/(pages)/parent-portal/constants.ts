/**
 * Parent Portal Constants
 * Only UI-related values (colors, templates)
 * All data is now fetched from API endpoints
 */

export const chartColors = {
  present: "#17a1fa",
  late: "#ffac01",
  absent: "#f92600",
};
